<!DOCTYPE html>
<html>
  <head>
    <title>YoutubeWatcher</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>

    <% # Pull in a standalone version of `react-player` package %>
<!--    <script src='https://cdn.jsdelivr.net/npm/react-player/dist/ReactPlayer.standalone.js'></script>-->
    <script src="https://www.youtube.com/iframe_api"></script>
  </head>

  <body>
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "" %>
    <% else %>
      <%= button_to "Login", new_user_session_path, class: "" %>
    <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
